# gradle-openapi-module-generator

The Idea of this plugin is to generate transient gradle submodules for given openapi specs that contain corresponding 
client libraries and can directly be used as dependencies and published with in the rest of your project.
